# Application dependencies

require:
    - play
    - constretto -> constretto

repositories:
    - Local modules:
        type:       local
        artifact:   ${application.path}/../[module]
        contains:
            - constretto


